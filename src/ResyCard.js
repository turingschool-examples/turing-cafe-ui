import React from 'react';
import PropTypes from 'prop-types';
import './ResyCard.css';

const Card = (props) => (
 // {name, date, time, numberOfGuests} = this.props
      <div>
        {this.props.name},
        {this.props.date},
        {this.props.time},
        {this.props.numberofGuests}
        <input type='submit' value=''/>
      </div>
)